<?xml version="1.0" encoding="utf-8"?>
<!--
    dialog_provisioning.xml — redesigned
    - An icon at the top
    - The current step's name + counter (step/total)
    - A clear progress bar
    - A monospace log inside a ScrollView
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="20dp"
    android:paddingEnd="20dp"
    android:paddingTop="4dp"
    android:paddingBottom="16dp">

    <!-- ── The user icon ── -->
    <ImageView
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="14dp"
        android:src="@drawable/ic_user"
        android:tint="?attr/colorPrimary"
        android:alpha="0.85" />

    <!-- ── The current step ── -->
    <TextView
        android:id="@+id/provision_step_tv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/setting_up"
        android:textAppearance="?attr/textAppearanceTitleSmall"
        android:textColor="?attr/colorOnSurface"
        android:gravity="center"
        android:layout_marginBottom="10dp" />

    <!-- ── The progress bar ── -->
    <com.google.android.material.progressindicator.LinearProgressIndicator
        android:id="@+id/provision_progress"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="6dp"
        android:indeterminate="false"
        android:max="100"
        android:progress="0" />

    <!-- ── The step counter ── -->
    <TextView
        android:id="@+id/provision_counter_tv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="0 / 0"
        android:textAppearance="?attr/textAppearanceLabelSmall"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:gravity="center"
        android:layout_marginBottom="14dp" />

    <!-- ── The step log ── -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="140dp">

        <TextView
            android:id="@+id/provision_log_tv"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:textSize="11sp"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:lineSpacingExtra="3dp"
            android:paddingHorizontal="2dp" />
    </ScrollView>

</LinearLayout>
