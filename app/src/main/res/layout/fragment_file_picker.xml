<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout for InternalFilePickerFragment
    Contains all the required IDs:
      picker_toolbar, picker_breadcrumb_scroll, picker_breadcrumb,
      picker_storage_chips, picker_recycler, picker_empty, picker_confirm_btn
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?attr/colorSurface">

    <!-- ── Toolbar ── -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/picker_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        app:navigationIcon="@drawable/ic_arrow_back"
        app:navigationContentDescription="@string/back"
        android:background="?attr/colorSurface"
        app:titleTextColor="?attr/colorOnSurface"/>

    <!-- ── Storage root chips ── -->
    <com.google.android.material.chip.ChipGroup
        android:id="@+id/picker_storage_chips"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="12dp"
        android:paddingVertical="6dp"
        app:singleSelection="true"
        app:selectionRequired="true"/>

    <!-- ── Breadcrumb scroll ── -->
    <HorizontalScrollView
        android:id="@+id/picker_breadcrumb_scroll"
        android:layout_width="match_parent"
        android:layout_height="36dp"
        android:scrollbars="none"
        android:paddingHorizontal="12dp"
        android:paddingVertical="4dp"
        android:background="?attr/colorSurfaceVariant">

        <LinearLayout
            android:id="@+id/picker_breadcrumb"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:gravity="center_vertical"/>

    </HorizontalScrollView>

    <!-- ── File list ── -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/picker_recycler"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingVertical="4dp"
            android:paddingBottom="80dp"/>

        <!-- Empty state -->
        <TextView
            android:id="@+id/picker_empty"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/no_files_found"
            android:textSize="15sp"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:visibility="gone"/>

    </FrameLayout>

    <!-- ── Confirm button (multi-select mode only) ── -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/picker_confirm_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="12dp"
        android:text="@string/confirm_selection"
        android:visibility="gone"
        style="@style/Widget.Material3.Button"/>

</LinearLayout>
