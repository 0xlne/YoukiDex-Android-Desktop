<?xml version="1.0" encoding="utf-8"?>
<!--
    fragment_workshop_detail.xml
    صفحة تفاصيل الخلفية — تصميمك الخاص بالكامل، مو WebView

    البنية:
    ┌─────────────────────────────────┐
    │  [← back]          [quality]   │  ← header شفاف فوق الصورة
    │                                 │
    │         thumbnail / video       │  ← 60% من الشاشة
    │              [▶ play]           │
    ├─────────────────────────────────┤
    │  العنوان                        │  ← bottom sheet تصميمك
    │  المصدر                         │
    │  الوصف (اختياري)                │
    │                                 │
    │  [  تحميل ⬇  ]                  │
    └─────────────────────────────────┘
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface">

    <!-- ── منطقة الفيديو/الصورة (60% من الشاشة) ────────────────────────── -->
    <FrameLayout
        android:id="@+id/detail_media_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#000000"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHeight_percent="0.58">

        <!-- Thumbnail (يظهر فوراً، يختفي عند تشغيل الفيديو) -->
        <ImageView
            android:id="@+id/detail_thumbnail"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:contentDescription="@null" />

        <!-- SurfaceView للفيديو (مخفي حتى يضغط play) -->
        <SurfaceView
            android:id="@+id/detail_surface_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone" />

        <!-- Loading spinner أثناء جلب التفاصيل -->
        <com.google.android.material.progressindicator.CircularProgressIndicator
            android:id="@+id/detail_loading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="visible"
            app:indicatorColor="@color/white"
            app:trackColor="#55FFFFFF" />

        <!-- زر Play في المنتصف -->
        <ImageView
            android:id="@+id/detail_play_button"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_play"
            android:background="@drawable/circle_bg_semi"
            android:padding="14dp"
            android:visibility="gone"
            android:contentDescription="تشغيل المعاينة" />

    </FrameLayout>

    <!-- ── Header شفاف فوق الصورة (back + quality badge) ───────────────── -->
    <ImageView
        android:id="@+id/detail_back_button"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_margin="12dp"
        android:src="@drawable/ic_arrow_back"
        android:background="@drawable/circle_bg_semi"
        android:padding="8dp"
        android:contentDescription="رجوع"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <TextView
        android:id="@+id/detail_quality"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="12dp"
        android:paddingHorizontal="10dp"
        android:paddingVertical="4dp"
        android:textSize="11sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:background="@drawable/badge_quality_bg"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:text="4K" />

    <!-- ── معلومات الخلفية (تحت الصورة) ────────────────────────────────── -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/detail_media_container"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp">

            <!-- العنوان -->
            <TextView
                android:id="@+id/detail_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="?attr/colorOnSurface"
                android:lineSpacingMultiplier="1.2"
                tools:text="Anime Girl Cyber Flame Rage Live Wallpaper" />

            <!-- اسم المصدر -->
            <TextView
                android:id="@+id/detail_source"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:paddingHorizontal="10dp"
                android:paddingVertical="3dp"
                android:textSize="12sp"
                android:textColor="?attr/colorPrimary"
                android:background="@drawable/badge_source_bg"
                tools:text="Moewalls" />

            <!-- الوصف (يظهر بعد جلب التفاصيل) -->
            <TextView
                android:id="@+id/detail_description"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:textSize="13sp"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:lineSpacingMultiplier="1.4"
                android:maxLines="4"
                android:ellipsize="end"
                android:visibility="gone"
                tools:text="Anime Girl Cyber Flame Rage posted in Anime category, original resolution 3840x2160" />

            <!-- زر التحميل -->
            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp">

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/detail_download_button"
                    android:layout_width="match_parent"
                    android:layout_height="52dp"
                    android:text="تحميل"
                    android:textSize="15sp"
                    app:cornerRadius="26dp" />

                <com.google.android.material.progressindicator.CircularProgressIndicator
                    android:id="@+id/detail_download_progress"
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:layout_gravity="center_vertical|end"
                    android:layout_marginEnd="14dp"
                    android:visibility="gone"
                    app:indicatorSize="28dp"
                    app:trackThickness="3dp" />

            </FrameLayout>

            <!-- مسافة سفلية -->
            <View
                android:layout_width="match_parent"
                android:layout_height="24dp" />

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>
