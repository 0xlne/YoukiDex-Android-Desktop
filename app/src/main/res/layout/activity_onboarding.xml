<?xml version="1.0" encoding="utf-8"?>
<!--
    Onboarding wizard container. A ViewPager2 hosts each step as its own Fragment
    (OnboardingWelcomeFragment, OnboardingPermissionsFragment, OnboardingProfileFragment,
    OnboardingRendererChoiceFragment, OnboardingDoneFragment), each entering/exiting with
    MaterialSharedAxis.X (verified against the real material-components-android source —
    MaterialSharedAxis extends MaterialVisibility<VisibilityAnimatorProvider>, the correct
    pattern per the library's own Motion docs for "steps with a navigational relationship").
    Page dots reuse the exact same visual language as the app drawer's own page indicator
    (page_dot_active/page_dot_inactive) for consistency across the app.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?attr/colorSurface"
    android:fitsSystemWindows="true">

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/onboarding_pager"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />
    <!-- userInputEnabled=false is set programmatically in OnboardingActivity.kt because
         it's not a valid XML attribute for ViewPager2 (it's a Kotlin/Java property).
         Navigation is deliberate (Back/Next buttons, or a step's own explicit action),
         not swipeable — a permissions step or profile-setup step being accidentally
         swiped past would be a real usability problem, unlike the app drawer's pages
         which are safe to swipe freely. -->

    <LinearLayout
        android:id="@+id/onboarding_page_dots"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="16dp"
        android:orientation="horizontal"
        android:gravity="center" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingBottom="24dp"
        android:gravity="center_vertical">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/onboarding_back_btn"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/back"
            android:visibility="invisible" />

        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/onboarding_next_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/next" />

    </LinearLayout>

</LinearLayout>
